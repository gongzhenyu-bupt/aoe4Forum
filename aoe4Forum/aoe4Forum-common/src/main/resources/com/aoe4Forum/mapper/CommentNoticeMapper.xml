<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aoe4Forum.mapper.CommentNoticeMapper">

    <resultMap id="CommentNoticeResultMap" type="com.aoe4Forum.entity.notice.CommentNotice">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="senderId" column="sender_id"/>
        <result property="businessId" column="business_id"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <insert id="insert" parameterType="map">
        INSERT INTO comment_notice (id, user_id, business_id,  create_time,sender_id)
        VALUES (#{id}, #{userId}, #{businessId}, #{createTime},#{senderId})
    </insert>

    <!-- 批量插入 -->
    <insert id="batchInsert" parameterType="map">
        INSERT INTO comment_notice (id, user_id, business_id, create_time,sender_id)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.userId}, #{item.businessId},#{item.createTime},#{item.senderId})
        </foreach>
    </insert>

    <select id="cursorQuery" resultMap="CommentNoticeResultMap">
        SELECT * FROM comment_notice
        WHERE user_id = #{request.userId}
        <if test="request.lastCreateTime != null and request.lastId != null">
            AND (
            create_time &lt; #{request.lastCreateTime}
            OR (create_time = #{request.lastCreateTime} AND id &lt; #{request.lastId})
            )
        </if>
        ORDER BY create_time DESC, id DESC
        LIMIT #{request.pageSize}
    </select>

</mapper>
