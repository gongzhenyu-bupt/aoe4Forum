<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aoe4Forum.mapper.FollowRelationMapper">

    <!-- 结果映射 -->
    <resultMap id="FollowRelationResultMap" type="com.aoe4Forum.entity.FollowRelation">
        <id property="id" column="id" />
        <result property="follower" column="follower" />
        <result property="followee" column="followee" />
        <result property="createTime" column="create_time" />
        <result property="deleteMark" column="delete_mark" />
    </resultMap>

    <!-- 查询某用户关注的人（分页游标方式） -->
    <select id="queryByFollower" resultMap="FollowRelationResultMap">
        SELECT * FROM follow_relation
        WHERE follower = #{request.userId}
        AND delete_mark = 0
        <if test="request.lastCreateTime != null and request.lastId != null">
            AND (
            create_time &lt; #{request.lastCreateTime}
            OR (create_time = #{request.lastCreateTime} AND id &lt; #{request.lastId})
            )
        </if>
        ORDER BY create_time DESC, id DESC
        LIMIT #{request.pageSize}
    </select>

    <!-- 查询某用户的粉丝（分页游标方式） -->
    <select id="queryByFollowee" resultMap="FollowRelationResultMap">
        SELECT * FROM follow_relation
        WHERE followee = #{request.userId}
        AND delete_mark = 0
        <if test="request.lastCreateTime != null and request.lastId != null">
            AND (
            create_time &lt; #{request.lastCreateTime}
            OR (create_time = #{request.lastCreateTime} AND id &lt; #{request.lastId})
            )
        </if>
        ORDER BY create_time DESC, id DESC
        LIMIT #{request.pageSize}
    </select>

    <!-- 插入关注关系 -->
    <insert id="insertFollowRelation" parameterType="com.aoe4Forum.entity.FollowRelation">
        INSERT INTO follow_relation (follower, followee, create_time, delete_mark)
        VALUES (#{follower}, #{followee}, #{createTime}, 0)
    </insert>

    <!-- 逻辑删除关注关系 -->
    <update id="deleteFollowRelation" parameterType="com.aoe4Forum.entity.FollowRelation">
        UPDATE follow_relation
        SET delete_mark = 1
        WHERE follower = #{follower} AND followee = #{followee}
    </update>
    <!-- 恢复关注关系 -->
    <update id="restoreFollowRelation" parameterType="com.aoe4Forum.entity.FollowRelation">
        UPDATE follow_relation
        SET delete_mark = 0
        WHERE follower = #{follower} AND followee = #{followee}
    </update>
    <select id="queryByFollowerAndFollowee" resultMap="FollowRelationResultMap">
        SELECT * FROM follow_relation
        WHERE follower = #{follower}
          AND followee = #{followee}
    </select>

    <!-- FollowRelationMapper.xml -->
    <select id="scanAllFollowRelations" resultMap="FollowRelationResultMap">
        SELECT follower, followee FROM follow_relation
        WHERE delete_mark = 0
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="queryAllByFollowee" resultMap="FollowRelationResultMap">
        SELECT * FROM follow_relation
        WHERE followee = #{followee}
    </select>

</mapper>
