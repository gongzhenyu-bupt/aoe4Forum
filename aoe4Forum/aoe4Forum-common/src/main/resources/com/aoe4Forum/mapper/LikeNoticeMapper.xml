<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aoe4Forum.mapper.LikeNoticeMapper">

    <resultMap id="LikeNoticeResultMap" type="com.aoe4Forum.entity.notice.LikeNotice">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="businessId" column="business_id"/>
        <result property="businessType" column="business_type"/>
        <result property="senderId" column="sender_id"/>
        <result property="senderName" column="sender_name"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <!--单独插入-->
    <insert id="insert" parameterType="map">
        INSERT INTO like_notice (id, user_id, business_id, sender_id, sender_name, create_time,business_type)
        VALUES (#{id},#{userId},#{businessId},#{senderId},
                #{senderName}, #{createTime},#{businessType})
    </insert>

    <!-- 批量插入 -->
    <insert id="batchInsert" parameterType="map">
        INSERT INTO like_notice (id, user_id, business_id, sender_id, sender_name, create_time,business_type)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.userId}, #{item.businessId}, #{item.senderId},
            #{item.senderName},  #{item.createTime}, #{item.businessType})
        </foreach>
    </insert>

    <!-- 游标分页查询 -->
    <select id="cursorQuery" resultMap="LikeNoticeResultMap">
        SELECT * FROM like_notice
        WHERE user_id = #{request.userId}
        <if test="request.lastCreateTime != null and request.lastId != null">
            AND (
            create_time &lt; #{request.lastCreateTime}
            OR (create_time = #{request.lastCreateTime} AND id &lt; #{request.lastId})
            )
        </if>
        ORDER BY create_time DESC, id DESC
        LIMIT #{request.pageSize}
    </select>

</mapper>
